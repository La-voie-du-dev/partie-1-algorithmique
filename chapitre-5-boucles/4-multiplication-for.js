// Multiplication de 5 * 10
let multiplicand = 5;
let multiplier = 10;

let product = 0;

// La syntaxe du for est valide sur plusieurs lignes
for (
  let count = 1;
  count <= multiplier;
  count++
) {
  product += multiplicand;
}


// Vérification
console.log(product);
